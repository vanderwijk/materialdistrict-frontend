# Open issues — patch Board picker

> Append aan `docs/open-issues.md`. Datum: 04-06-2026.

## Opgelost
- **BD-1 — Boards "Add to board"** — frontend werkend (BoardPickerModal + GET
  boards + items-proxy; vier detail-wrappers aangesloten). Backend stond al live
  (Johan 2aedda2). Daarmee zijn bookmarks, saved searches én boards rond.

## 🟢 Klein / later
- Save-knop op **book-** en **brand-detail**: nog geen UI-affordance (bookmark-
  en board-types ondersteunen ze al). Toevoegen wanneer die detailpagina's een
  Save/Add-to-board-rij krijgen.
- Item **verwijderen** uit een board: endpoint + UI nog niet gebouwd (niet nodig
  voor v1).

## Roadmap (ongewijzigd)
- Channel-hubs `/channels` + `/channels/[slug]` — build-order Stap 12.
