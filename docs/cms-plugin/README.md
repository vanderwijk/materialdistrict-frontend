# CMS plugin docs (Claude-readable mirror)

These documents live here so Claude can read them. The `materialdistrict-plugin`
GitHub repo is private; this frontend repo is the shared workspace.

**Canonical classification rulebook for Claude:**
`docs/materiaal-classificatie-regelboek.md` (repo root `docs/`, not only this folder).

Operational PHP one-offs, import CSVs and exports with personal data stay in the
private plugin repo / on the CMS server — they are intentionally not mirrored here.
